module.exports = {
  // 单行最大字数
  printWidth: 120,
  //句末加分号
  semi: true,
  // 统一使用单引号，
  // jsx内属性用双引号，后面有配置
  singleQuote: true,
  // 在换了行的数组,对象项末尾加 ,
  trailingComma: 'all',
  // 在{} 前后都加空格， 例如{ a: 1, b: 2 }
  bracketSpacing: true,
  // 多行jsx 以 >为最后一行
  bracketSameLine: false,
  // 为单行箭头函数的单个参数，不需要加括号
  arrowParens: 'avoid',
  // 缩进 2个空格
  tabWidth: 2,
  // 不使用Tab缩进，而使用空格
  useTabs: false,
  // 文件结尾要换行
  endOfLine: 'auto',
};
